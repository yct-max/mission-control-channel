{
  "id": "mission-control",
  "kind": "agent",
  "name": "Mission Control",
  "description": "Mission Control channel plugin for OpenClaw — MC as a native channel. Requires agentToken — create an integration via POST /api/agent-integrations in Mission Control.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "mcUrl": {
        "type": "string",
        "description": "Mission Control base URL, e.g. http://100.78.2.112:18793"
      },
      "gatewayUrl": {
        "type": "string",
        "description": "Public gateway URL for this agent, e.g. http://100.113.226.91:18789. Used to register the webhook with MC."
      },
      "agentToken": {
        "type": "string",
        "description": "Legacy: single token for all agents. Use agents{} for per-agent tokens instead."
      },
      "agents": {
        "type": "object",
        "description": "Per-agent token map. Key is agent name (alex, max, quinn). Value.token is the MC integration token.",
        "additionalProperties": {
          "type": "object",
          "additionalProperties": false,
          "required": ["token"],
          "properties": {
            "token": { "type": "string" },
            "displayName": { "type": "string" }
          }
        }
      },
      "webhookPath": {
        "type": "string",
        "description": "Webhook path for MC to post events (default: /mc/webhook)"
      }
    }
  },
  "version": "0.2.0",
  "sdk": "openclaw",
  "status": "enabled"
}
