{
  "name": "@yct-max/openclaw-mission-control",
  "version": "0.2.1",
  "description": "Mission Control channel plugin for OpenClaw — MC as a native channel",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepack": "tsc"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "peerDependencies": {
    "openclaw": ">=1.0.0"
  }
}
